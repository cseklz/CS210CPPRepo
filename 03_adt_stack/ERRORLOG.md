# ERRORLOG
[18:32] Only error I ran into was my testing failing from a push that wasn't popped.
I went into the debugger and looked at the stack to find the value.

There weren't a lot of errors, this wasn't too hard.